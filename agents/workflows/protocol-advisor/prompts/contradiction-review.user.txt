TASK MODE: contradiction_review

Review this protocol at the whole-document level for contradictory or inconsistent statements across sections.

Compare sections against each other, not against template requirements.

Return JSON with this shape:
{{output_json_example}}

Input:
{{input_json}}
