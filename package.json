{
  "name": "research-optimizer",
  "private": true,
  "type": "module",
  "workspaces": [
    "shared",
    "database",
    "gateway",
    "cms",
    "agents",
    "users",
    "server"
  ],
  "scripts": {
    "start": "npm start -w server",
    "start:gateway": "npm start -w gateway",
    "start:cms": "npm start -w cms",
    "start:agents": "npm start -w agents",
    "start:users": "npm start -w users",
    "test": "npm test -w server",
    "lint": "eslint . --cache --cache-location ./node_modules/.cache/eslint/",
    "lint:ci": "eslint . --cache --cache-location ./node_modules/.cache/eslint/ --max-warnings 0",
    "lint:fix": "eslint . --fix --cache --cache-location ./node_modules/.cache/eslint/",
    "db": "npx @electric-sql/pglite-socket --db=./data --port=5433 --host 0.0.0.0",
    "db:sql": "psql \"postgresql://postgres:postgres@127.0.0.1:5433/postgres?sslmode=disable&gssencmode=disable\"",
    "db:generate": "drizzle-kit generate",
    "db:audit-vectors": "node database/scripts/audit-vector-schema.js",
    "db:audit-relations": "node database/scripts/audit-relational-integrity.js",
    "format": "prettier . --check",
    "format:fix": "prettier . --write",
    "prepare": "husky || true"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,css,html}": [
      "prettier --write"
    ]
  },
  "overrides": {
    "eslint-plugin-import-x": {
      "eslint": "$eslint"
    }
  },
  "devDependencies": {
    "@eslint/compat": "^2.0.5",
    "@eslint/js": "^10.0.1",
    "drizzle-kit": "^0.31.10",
    "eslint": "^10.2.0",
    "eslint-plugin-import-x": "^4.16.2",
    "globals": "^17.4.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.2"
  }
}
